import 'package:flutter/material.dart';

class AppBorders {
  static const border = Border.fromBorderSide(BorderSide(
    color: Color(0xff6b62cc),
    width: 1,
  ));

  static const borderNew = Border.fromBorderSide(BorderSide(
    color: Color(0xff3a79bd),
    width: 12,
  ));

  static const token = Border.fromBorderSide(BorderSide(
    color: Color(0xff3a79bd),
    width: 8,
  ));

  AppBorders._();
}
