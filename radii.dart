import 'package:flutter/material.dart';

class AppRadii {
  static const radii = BorderRadius.all(Radius.circular(2));

  static const radiiReferenced = BorderRadius.all(Radius.circular(2));

  AppRadii._();
}
