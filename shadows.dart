import 'package:flutter/material.dart';

class AppShadows {
  static const darkKnight = BoxShadow(
    color: Color(0xffffffff),
    offset: Offset(1, 2),
    blurRadius: 3,
    spreadRadius: 4,
  );

  AppShadows._();
}
